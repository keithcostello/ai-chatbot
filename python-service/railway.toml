# Railway configuration for SteerTrue Pydantic AI Microservice
#
# Reference: CONTEXT.md lines 152-158 - Railway readiness probe

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
# Use sh -c for proper variable expansion
startCommand = "sh -c 'uvicorn main:app --host 0.0.0.0 --port ${PORT:-8000}'"
healthcheckPath = "/health"
healthcheckTimeout = 5
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
